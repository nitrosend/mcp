{
  "name": "@nitrosend/mcp",
  "version": "0.6.2",
  "description": "MCP server for Nitrosend — connect Claude to your email marketing platform",
  "mcpName": "io.github.nitrosend/mcp",
  "license": "MIT",
  "type": "module",
  "bin": {
    "nitrosend-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "dev": "tsc --watch"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.0"
  }
}
